id: py-exists-gated-is-symlink
snapshots:
  ? |
    from pathlib import Path

    def reject_symlink(path: Path) -> None:
        if path.exists() and path.is_symlink():
            raise ValueError("Symlinks are not allowed")
  : labels:
    - source: path.exists() and path.is_symlink()
      style: primary
      start: 73
      end: 108
